#ifndef DEF_MENU
#define DEF_MENU

#include "shapes.h"

/**
 * @brief Menu principal permettant d'acceder aux sous-menus de creation,
 *  edition, suppression et sauvegarde des formes ainsi que de quitter le programme.
 * 
 * @param array Tableau de stockage des formes.
 * @param viewbox Fenetre de visualisation.
 */
void menu_for_user(array_t *array, viewbox_t *viewbox);


#endif